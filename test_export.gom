// Test enhanced export serialization

// Simple values
const var score = 42!
export score to "exported_score.gom"!

const var message = "Hello, World"!
export message to "exported_message.gom"!

// Array with nested values
const var numbers = [1, 2, 3, 4, 5]!
export numbers to "exported_numbers.gom"!

// Mixed array
const var mixed = [42, "text", true, false]!
export mixed to "exported_mixed.gom"!

print("Export tests completed!")!